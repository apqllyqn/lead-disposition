# Lead Disposition System

Contact state machine with cooldown logic, TAM tracking, and campaign fill engine.

## Setup

```bash
pip install -e ".[dev]"
disposition-init-db
```

## Environment

```
DISPOSITION_DATABASE_URL=postgresql://brainon:brainon2025@localhost:5433/disposition_db
```
